<Window x:Class="Encryption.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Encryption App" Height="450" Width="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select Encryption Algorithm:" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <RadioButton x:Name="rbReverse" Content="Reverse" IsChecked="True" Margin="0,0,20,0" GroupName="Algorithm" Checked="RadioButton_Checked"/>
            <RadioButton x:Name="rbName1" Content="ROT13" Margin="0,0,20,0" GroupName="Algorithm" Checked="RadioButton_Checked"/>
            <RadioButton x:Name="rbName2" Content="Name2" GroupName="Algorithm" Checked="RadioButton_Checked"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Plain Text" Margin="0,0,0,10">
            <TextBox x:Name="txtInput" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button x:Name="btnEncrypt" Content="Encrypt" Width="100" Margin="0,0,10,0" Click="btnEncrypt_Click"/>
            <Button x:Name="btnDecrypt" Content="Decrypt" Width="100" Click="btnDecrypt_Click"/>
        </StackPanel>

        <GroupBox Grid.Row="4" Header="Cypher Text" Margin="0,10,0,0">
            <TextBox x:Name="txtOutput" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </GroupBox>
    </Grid>
</Window>